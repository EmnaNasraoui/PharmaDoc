import { FiltrePricePipe } from './filtre-price.pipe';

describe('FiltrePricePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrePricePipe();
    expect(pipe).toBeTruthy();
  });
});
