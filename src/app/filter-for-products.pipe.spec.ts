import { FilterForProductsPipe } from './filter-for-products.pipe';

describe('FilterForProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterForProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
