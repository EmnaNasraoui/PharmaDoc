import { CheckBoxFiltrePipe } from './check-box-filtre.pipe';

describe('CheckBoxFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckBoxFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
